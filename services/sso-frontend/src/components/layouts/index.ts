/**
 * Barrel export — layouts layer (design.md §3.1).
 *
 * Layouts = shell yang membungkus pages (sidebar, header, footer).
 */

export { default as AuthLayout } from './AuthLayout.vue'
export { default as PortalLayout } from './PortalLayout.vue'

import { TablePageLoading } from "@/components/PageLoading";

export default function Loading() {
  return (
    <TablePageLoading
      title="Loading sessions"
      description="Refreshing active sessions across every registered app."
    />
  );
}
